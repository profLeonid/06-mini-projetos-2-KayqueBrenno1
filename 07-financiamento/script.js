'use strict'

const qtdeMeses = function(quantidade){
    let meses = []

    for(let i = 1; i <= quantidade; i++)
        meses.push(i)
}

const calcularParcela = (valorTotal, qtdeParcela) => Number(valorTotal / qtdeParcela)
